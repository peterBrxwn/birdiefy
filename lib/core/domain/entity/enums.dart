enum Status { error, initial, loading, success }
enum ButtonColorEnum { error, grey, primary, transparent, white }
enum ModeEnum { dark, light, system }
enum AppStatus { authenticated, unauthenticated }
enum StringToDate {
  allTime,
  aMonthAgo,
  aYearAgo,
  nextMonth,
  thisMonth,
  thisQuarter,
  thisWeek,
  thisYear,
  today,
  tomorrow,
  tomorrowNextMonth,
  yesterday,
}
enum DataStatusEnum { unchanged, newData, update, delete, doesNotExist }
